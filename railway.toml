[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build:dashboard && npx tsx bot-with-dashboard.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
